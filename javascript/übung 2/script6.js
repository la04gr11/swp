// Zähle von 0 auf 10, dann gib Deinen Namen aus, dann zähle von 10 auf 0 
for(let zahlen = 0; zahlen <= 10; zahlen++){
    console.log(zahlen)
}
 console.log("lazar")

 for(let count = 10; count >= 0; count--){
    console.log(count)
}
